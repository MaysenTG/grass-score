<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <%= link_to "Grass score", root_path, class: "navbar-brand" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <% unless account_signed_in? %>
          <li class="nav-item">
            <%= link_to "Home", root_path, class: "nav-link #{'active' if current_page?(root_path)}" %>
          </li>
        <% end %>
        <% if account_signed_in? %>
          <li class="nav-item">
            <%= link_to "Games", games_path, class: "nav-link #{'active' if current_page?(games_path)}" %>
          </li>
          <li class="nav-item">
            <%= link_to "Create a new game", new_game_path, class: "nav-link #{'active' if current_page?(new_game_path)}" %>
          </li>
          <li class="nav-item">
            <%= link_to "Users", users_path, class: "nav-link #{'active' if current_page?(users_path)}" %>
          </li>
          <li class="nav-item">
            <%= link_to "Round reports", reports_path, class: "nav-link #{'active' if current_page?(reports_path)}" %>
          </li>
        <% end %>
      </ul>

      <ul class="navbar-nav">
        <% if current_account %>
          <li class="nav-item">
            <%= link_to "Profile", edit_account_registration_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= button_to "Log out", destroy_account_session_path, method: :delete, form: { data: { turbo_confirm: "Are you sure you want to log out?" } }, class: "nav-link" %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Log in", new_account_session_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "Sign up", new_account_registration_path, class: "nav-link" %>
          </li>
        <% end %>
    </div>
  </div>
</nav>
